title: <single-concept container name>
parent: <parent issue title, or ->
labels: <comma-separated facet labels, or ->
milestone: <milestone title, or ->
---
<body: scope plus current decision, prose never hard-wrapped>

This file documents the input format for `seed.py` and is the only template committed here. The actual seeding packet — one file per issue in this format, named `NN-slug.md` for ordering — is local input and is never committed: issue content lives on the tracker (persistence doctrine), and a committed copy would be a second source of truth that drifts.
